@model Boolean? 
@{ 
    var options = new List<SelectListItem>();
    options.Add(new SelectListItem() { Text = "全", Value = "" });
    options.Add(new SelectListItem() { Text = "上線", Value = true.ToString() });
    options.Add(new SelectListItem() { Text = "待審", Value = false.ToString() });

    string css = ViewBag.@class ?? "";
}

@Html.DropDownListFor(m => m, options, new { @class = css })
